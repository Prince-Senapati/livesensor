import logging 
import os
import sys
from datetime import datetime

logging.basicConfig(
    filename=,
    format="[%(asctime)s] %(lineno)d %(name)s - %(levelname)s - %(message)s",
    level=logging.INFO()
)